\chapter{Regarding Proposals and Hypotheses}

Given the results previously shown, I permet myself to extend
some conjectures and hypotheses to which I reached. In addition
to offer some possible approaches to the problem.

\section{Conjectures and Hypotheses}
Given the nature of the model, the lack of boundaries didn't
affect considerably the results. Which I think is part of what
is called \textbf{inevitability}. Such inevitability is an
interesting aspect form the computation point of view due to
\say{natural falling} into the correct values to minima without
the need of constraints.


\section{Proposals}

For a better minima search I propose to use the sqared
potential as part of the penalizations, since the behaviour
at minimization is to always fall on the root (this case, the
zero).
